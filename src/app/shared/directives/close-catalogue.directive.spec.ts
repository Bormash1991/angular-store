import { CloseCatalogueDirective } from './close-catalogue.directive';

describe('CloseCatalogueDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseCatalogueDirective();
    expect(directive).toBeTruthy();
  });
});
